# GAME_DESIGN — Pixel Arcade Tycoon

모든 수치는 초기값이며 밸런스 조정 대상. **수치 계산은 전부 결정론적** — 같은 seed + 같은 입력 = 같은 결과. LLM은 어떤 수치에도 관여하지 않는다.

## 1. 전체 플레이 흐름 (상태 기계)

```
[타이틀/코치마크] → [회의실 PLAN] → [회의 MEETING] → [결정 DECIDE] → (제작 완료)
   → [오락실 PLACE] → [영업 OPEN(시뮬)] → [결산 REPORT] → 다시 [회의실 PLAN(개선 안건)] → …
어느 상태에서든: 오락실 기기 클릭 → [직접 플레이 TRYOUT(러너만)] → 복귀
```

- 시간 단위: **주차(턴)**. 회의 결정의 "소요 일수"만큼 주차가 진행되고, 오락실 영업 1회 = 1주차.
- 세션 목표: 3분 내 1.5사이클 (제작 1회 + 영업 1회 + 개선 1회 + 영업 1회).

## 2. 게임(제작물) 데이터 모델

```
GameProject {
  id, name(LLM/풀 생성), version(v0.1→개선 시 +0.1),
  genre: runner | shooter | puzzle | rhythm,
  concept: 콘셉트 태그(스킨 결정), target: 손님 유형 1~2, features: 특징 1~2,
  stats: { fun 0..100, polish 0..100, difficulty 0..100, bugs: 정수 },
  price: 300..700₩(100 단위), rating 0.5..5.0(파생), plays, revenue,
  placed: 슬롯 index | null
}
```

### 기획 선택지 (회의실 PLAN — 각 3~4택)

| 축 | 선택지 | 효과(초기값) |
|---|---|---|
| 장르 | 액션 러너 / 슈팅 / 퍼즐 / 리듬 | 손님 유형 선호 매칭의 기준. 러너만 직접 플레이 지원 |
| 콘셉트 | 네온 시티 / 우주 / 유령 저택 / 캔디 랜드 | 스킨(캐비닛·썸네일 팔레트) + fun +4 (타깃 선호 콘셉트일 때) |
| 타깃 | 학생 / 대학생 / 직장인 / 마니아 | 해당 유형 만족 보정 +15, 반대 성향 -5 |
| 특징 | 콤보 대시 / 글로벌 랭킹 / 스토리 모드 / 초고난도 모드 | 아래 특징 표 |

특징 효과: 콤보 대시 fun+8·difficulty+5 / 글로벌 랭킹 대학생 재방문율 +20%p / 스토리 모드 polish+8·개발 +1주 / 초고난도 difficulty+20·마니아 +15·학생 -10.

기본 스탯: fun 40+장르 기본(러너 8, 리듬 6, 슈팅 5, 퍼즐 4), polish 30, difficulty 장르 기본(러너 45, 슈팅 55, 퍼즐 35, 리듬 50), bugs **2**.

**추가 확정값** (구현이 임의로 정하지 말 것):

| 항목 | 값 |
|---|---|
| 기본 가격 | 400₩ (PLAN 에서 별도 설정 없음 — 오락실 하단 바에서 ±100 조정) |
| 반대 성향 쌍 | 학생↔마니아, 대학생↔직장인 (타깃 지정 시 반대쪽 -5) |
| 유형별 선호 콘셉트 | 학생=네온 시티, 대학생=우주, 직장인=캔디 랜드, 마니아=유령 저택 (+8 콘셉트 매칭·fun+4 기준) |

## 3. 회의와 결정 (MEETING → DECIDE)

- 회의 연출: 팀원 4명 말풍선 3~5개 순차 재생(각 1.5초, 스킵 가능). 내용은 LLM/풀 — 선택한 기획과 팀원 성향을 반영.
- 안건: 매 제작·개선마다 **3택 trade-off** (하단 바). 항상 팀원 1명의 "추천" 배지가 붙는다 (성향 기반).

| 결정 | 효과 | 소요 |
|---|---|---|
| 재미 우선 | fun +18, bugs +2 (확정 — 숨은 확률 없음, 심사자가 체감할 수 있게) | 3주 |
| 완성도 우선 | polish +12, bugs -2 (최소 0) | 5주 |
| 빠른 출시 | 즉시 배치 가능, 평판 -5 리스크(버그≥3일 때) | 0주 |

주차는 공짜가 아니다: **주당 운영비 300₩** 이 주차 진행마다 자동 차감된다(HUD 코인에 -300 플로트 표시). 5주 결정 = -1,500₩ — 시간 vs 품질의 실질 trade-off.

개선(2회차+) 안건은 **피드백 태그 집계에서 자동 생성** — 상위 태그에 해당하는 행의 3택을 사용한다. 개선 회의의 말풍선은 **최대 2개**로 압축(템포 예산).

| 상위 태그 | 선택지 ① | 선택지 ② | 선택지 ③ |
|---|---|---|---|
| `-가격` | 가격 -100₩ (0주) | 콘텐츠 보강 fun +10 (3주) | 무시 (0주) |
| `-버그` | 대청소 bugs -3 (4주) | 핫픽스 bugs -1 (1주) | 무시 |
| `-조작`·`-난이도` | 난이도를 타깃 선호 중심으로 조정 (2주) | 판정 개선 polish +8 (3주) | 무시 |
| `랭킹 요청` | 글로벌 랭킹 특징 추가 (3주) | 점수판만 추가 fun +4 (1주) | 무시 |
| 기타/`+` 우세 | 기본 3택(재미/완성도/빠른 개선) 재사용 | | |

'무시'도 유효한 선택(즉시 재영업) — trade-off 는 시간·운영비다.

## 4. 팀원 에이전트 (고정 4명)

| 이름 | 역할 | 성향(추천 편향) | 초기 Lv | 개성 비주얼 |
|---|---|---|---|---|
| 도윤 | 기획 | 재미 우선 | 4 | 초록 셔츠·안경 (`ch:pl`) |
| 루나 | 디자인 | 콘셉트·polish | 3 | 분홍 머리 (`ch:ds`) |
| 준호 | 개발 | 빠른 출시(피곤할수록) | 5 | 후드 (`ch:dv`) |
| 세라 | QA | 완성도 우선 | 2 | 당고머리 (`ch:qa`) |

- 성장: 영업 결산마다 EXP +10~30 (자기 역할 관련 태그 발생 시 추가). 100 도달 → 레벨업 배지 → 담당 수치 보정 +2/Lv.
- **기억 카드(memory)**: 한 결산에서 **동일 태그가 2회 이상** 발생하면 해당 역할 팀원의 기억 카드 생성 (예: `랭킹 요청` ×2 → 도윤 기억 "대학생층은 경쟁을 좋아함"). 1.5사이클 심사 세션 안에 반드시 등장 가능한 문턱값. 최대 4장 표시. 효과: 관련 선택지에 힌트 배지 + 해당 보정 +3. LLM 대사 컨텍스트에 포함.
- 컨디션: 연속 3주 이상 제작 시 준호 '피곤' (polish 획득 -20%) — 시각 표현은 tired 표정.

## 5. 손님 에이전트 (4유형)

| 유형 | 색 | 선호 장르 | 선호 난이도 | 가격 민감도 | 습성 |
|---|---|---|---|---|---|
| 학생 | #4cc9f0 | 러너(액션) | 30~55 | 높음(500↑ 불만) | 무리 방문(2~3명 동시) |
| 대학생 | #57d99b | 리듬 | 40~65 | 중간 | 랭킹 특징 있으면 재방문 +20%p |
| 직장인 | #ffd23e | 퍼즐 | 25~50 | 낮음 | 평균 플레이 짧음, 만족 시 조용히 재방문 |
| 마니아 | #b388ff | 슈팅 | 60~85 | 낮음 | 같은 기기 연속 플레이, 조작감(버그) 민감 ×2 |

### 영업 시뮬 (OPEN, 1주차 = 약 15초 연출 + 화면 상단 ⏩ 2× 배속 버튼 상시 표시)

- 방문객 수 = 8 + floor(평판/50) + 2d4(seed RNG). 유형 분포: 학생 35% / 대학생 25% / 직장인 25% / 마니아 15%.
- 손님별 판정(결정론):
  - `base = 25 + fun*0.5 + polish*0.3 - bugs*4`
    (상수 25 는 "첫 게임이 그럭저럭 성공"을 보장하는 톤 조정값 — 골든 패스 평균 만족 ≈ 60대, 첫 별점 3.0~3.8 목표)
  - `+15` 선호 장르 매칭, `+15` 타깃 지정 매칭, `+8` 선호 콘셉트, `-|difficulty-선호중심|*0.4` 난이도 미스매치, `-12` 가격 민감 초과, 마니아는 bugs 페널티 2배
  - `만족도 = clamp(base, 0, 100)` → 감정 칩: ≥75 환호(하트/신남) · 45~74 보통 · <45 실망(구름/화남)
  - 별점 기여 = 만족도/20. 게임 rating = 최근 20회 이동평균. **버전 +0.1(개선) 시 rating 윈도우 초기화** — 신버전 플레이만 집계해 개선 체감이 구버전 기록에 희석되지 않게 한다.
- 매출: 만족도 ≥30이면 지불(가격만큼 코인 +). 재플레이: 만족 ≥75 & 습성 보정 통과 시 1회 추가.
- 피드백 카드: 손님별 확률 40%로 생성 — `대사(LLM/풀) + 태그`. 태그는 판정에서 파생(결정론): 가격 페널티 발생 → `-가격`, bugs≥3 → `-버그`, 콘셉트 매칭 → `+연출`, 랭킹 희망(대학생·랭킹 없음) → `랭킹 요청` 등.
- **재방문 memory**: 직전 사이클에 실망(<45)했던 유형은 다음 영업에서 **최소 1명 반드시 스폰**된다(재방문 보장). 같은 유형이 재방문했고 `이번 만족도 - 직전 사이클 만족도 ≥ +8` 이면 개선 언급 대사 발생. 조건을 충족한 유형의 **첫 손님에게 강제 발화** — 개선을 실행한 사이클에는 반드시 1회 이상 나온다(슬라이스의 핵심 증명 장면이므로 확률에 맡기지 않는다). 개선 항목 명칭(`{improvement}`)은 직전 개선 결정의 대상 수치에서 파생: fun→"재미", polish→"완성도/판정", bugs→"버그", price→"가격".

## 6. 자원과 경제

- 시작: 코인 10,000₩, 평판 100, 1주차.
- 지출: 신규 제작 3,000₩, 개선(업그레이드) 2,000₩ + 결정 소요 주차.
- 수입: 영업 매출. 평판: 결산 평균 만족도 ≥70 → +10, <40 → -10. 버그≥5로 영업 시 -15 ("치명적 버그" 경고).
- 파산 없음(슬라이스) — 코인 부족 시 해당 선택지 비활성("자금 부족") + 영업으로 벌라는 힌트.

## 7. 성공·실패·진행

- 명시적 게임오버 없음. 진행 동기 = 별점·평판·매출 성장, 기억 카드 수집, 팀원 레벨업.
- soft-lock 금지 조건: 어떤 상태에서도 "다음 행동" 버튼이 항상 1개 이상 활성화.
- 세션 종료 화면 없음 — 계속 플레이 가능. 3분 시점에 "여기까지가 데모 핵심" 배너는 넣지 않는다(심사자 자율).

## 8. 직접 플레이 (TRYOUT — 러너)

- 원버튼(탭/스페이스/클릭) 점프, 30~45초 런. 게임 스탯이 실제 파라미터로: difficulty → 장애물 밀도·속도, polish → 배경 레이어·파티클 유무, bugs → 의도적 판정 오차 연출(웃긴 버그: 가끔 점프가 2단 발동 등, 3건 이상일 때), concept → 스킨 팔레트.
- 종료 시 점수 → 해당 기기 "오늘 최고 점수"로 기록, 코인 미차감(사장 시연).
- 진입: 배치된 러너 기기 클릭 → "직접 플레이". 종료: 충돌 or ESC. **첫 결산 이후 러너 캐비닛 위에 "▶ 직접 플레이" 오버헤드 칩을 상시 표시** — 심사자가 가장 인상적인 기능을 놓치지 않게 (팝업 아님, ART_BIBLE 오버헤드 규칙).

## 9. 콘텐츠 구조 (데이터 파일)

- `src/data/choices.ts` — 기획 선택지·효과 수치 (§2)
- `src/data/personas.ts` — 팀원·손님 정의 (§4·§5)
- `src/data/decisions.ts` — 안건 템플릿과 효과 (§3)
- `src/data/pools/*.json` — 사전 생성 대사 풀 (`TECH_SPEC.md` §LLM)
- 밸런스 상수는 `src/data/balance.ts` 한 파일에 모은다 (조정 범위 주석 포함).

## 10. 밸런스 조정 가이드

- 1사이클(제작→영업→결산)이 90~120초를 넘으면: 말풍선 수·연출 시간을 먼저 줄인다 (수치가 아니라).
- 첫 게임이 평균 별점 3.0~3.8에 안착해야 개선의 여지가 보인다. 4.5+ 나오면 base 계수 하향.
- 개선 1회로 만족도 체감 +10~20 보장 (재방문 대사 트리거를 위해).
