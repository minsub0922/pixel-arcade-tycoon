# PLAYTEST_SCENARIOS — 자동 플레이 시나리오

구현 위치: `tests/playtest/` (Playwright). 공통 규칙:

- 각 시나리오는 `__GAME_TEST__.setSeed(<seed>)`로 시작 (명시 없으면 seed **42**).
- 입력은 실제 클릭 우선, 연출 대기는 `step(ms)` 허용. LLM 모드는 명시 없으면 `pool` 강제.
- 증거: 지정 시점 스크린샷 → `evidence/`, 실패 시 trace 저장. 제한 시간 초과 = 실패.
- soft-lock 판정: 5초간 활성화된 액션 버튼이 0개면 실패.

## S1 — 정상 3분 핵심 루프 (golden path)

- seed 42, 새 게임. 입력: 코치마크 닫기 → 장르 '액션 러너' → 콘셉트 '네온 시티' → 타깃 '학생' → 특징 '글로벌 랭킹' → 제작 시작 → 회의 스킵 없이 재생 → '재미 우선' → 오락실 이동 → 카드 배치 → 영업 시작 → 결산 확인 → 개선 안건 선택 → '완성도 우선' → 재영업 → 결산.
- 기대 상태: 사이클 ≥ 1.5, 게임 rating 산출, 코인 증가, 피드백 카드 ≥ 3, 태그 집계 존재, 2회차 결산에 재방문 개선 대사 ≥ 1.
- 기대 텔레메트리: 총 소요(연출 실시간 기준) ≤ 180s, 첫 게임 완성 ≤ 60s & 클릭 ≤ 8, LLM 호출 ≤ 3/사이클.
- 스크린샷: 회의(말풍선), 결정 바, 오락실 영업(손님+감정칩), 결산 패널, 재방문 대사. 제한 시간 240s.

## S2 — 결정 분기 비교 (선택이 의미 있는가)

- 동일 seed 7로 두 런: A='재미 우선', B='완성도 우선' (나머지 입력 동일).
- 기대: A.fun > B.fun, B.polish > A.polish, B.bugs ≤ A.bugs, 두 런의 결산 만족도 분포·피드백 태그가 서로 다름. 두 결산 화면 스크린샷 나란히 저장.

## S3 — 저성과와 회복

- seed 13. 최악 조합(장르 '슈팅' + 타깃 '학생' + 특징 '초고난도' + '빠른 출시') → 기대: 평균 만족 < 45, 실망 감정 칩 다수, 별점 ≤ 2.5, 평판 하락. 이어서 개선 2회 → 기대: 별점 +1.0 이상 회복, 회복 과정 스크린샷.
- soft-lock 없이 저성과 상태에서도 다음 행동 항상 존재해야 함.

## S4 — LLM 장애 폴백

- 케이스 a: pool 모드 강제(기본) — S1 전체 완주, 대사 전부 표시(빈 말풍선 0).
- 케이스 b: proxy 모드 + 네트워크 차단(Playwright route abort) — 타임아웃 2.5s 내 pool 폴백, 진행 끊김(스피너·에러 노출) 없음, `llmMode()` 폴백 기록 확인.
- 케이스 c: proxy가 500/깨진 JSON 반환 — 동일하게 무공지 폴백.

## S5 — 저장·이어하기

- S1을 1사이클까지 진행 → 상태 A 스냅샷 → 페이지 리로드 → 기대: 이어하기로 상태 A 복원(코인·게임·기억 카드·주차 동일). '새 게임' 선택 시 초기화 확인. localStorage를 깨진 JSON으로 오염 후 리로드 → 크래시 없이 새 게임.

## S6 — 연속 5사이클 안정성·성장

- seed 99, 자동 입력으로 5사이클 연속(입력 스크립트는 결정 로테이션). 기대: 크래시 0, 콘솔 에러 0, 수치 범위 유지(fun/polish ≤ 100, 코인 ≥ 0, rating 0.5~5.0), 팀원 레벨업 ≥ 1회, 기억 카드 ≥ 1장 생성, FPS 표본 평균 ≥ 50. 제한 시간 420s (step 가속 허용).

## S7 — 러너 직접 플레이 (TRYOUT)

- S1 1사이클 후 러너 기기 클릭 → 직접 플레이 → 30초 자동 입력 봇(점프 타이밍 랜덤 seed) → 기대: 조작 반응 < 100ms(입력→점프 프레임), 종료 후 오락실 복귀, 최고 점수 기록 표시, ESC 중도 이탈도 정상 복귀. difficulty 20 vs 80 두 설정에서 장애물 밀도 차이 어서션. 스크린샷 2종.

## 커버리지 매핑

QUALITY_BAR A3~A8 ← S1~S7 / tasks.yaml 각 태스크의 Verification이 이 시나리오 id를 참조한다. 시나리오 수정 시 이 문서를 먼저 갱신하고 테스트 코드를 맞춘다.
