# DECISIONS — 결정 로그

형식: 날짜 · 결정 · 근거 · 대안(기각 이유)

## 2026-08-09 (초기 인터뷰, 사용자 확정)

1. **용도 = 사전과제 제출 (D-1)** — Wannabe에서 피벗. 수직 슬라이스가 곧 제출물. 영상·PDF·배포가 태스크에 포함.
2. **핵심 재미 = 창작→반응→개선 루프** — AI 손님의 개성이 최대 볼거리. 대안 A(팀 관전 중심)는 3분 템포와 긴장 관계, B(경영 중심)는 AI 체감 약화로 기각.
3. **게임 실체 = 구동되는 템플릿 미니게임** — 러너 1종만 직접 플레이. LLM 코드 생성(기각: 실패율·검증 불가), 완전 추상 시뮬(기각: 시연 임팩트 부족).
4. **LLM = Gemini + Workers 무료 프록시 + 사전 생성 풀 폴백** — 공고의 "유료 키 없이 심사자 실행" 요건 때문에 클라이언트 키 내장 불가(정적 빌드에 노출). Claude 유료 라이브(기각: 요건 위반), 사전 생성만(기각: 라이브 AI 임팩트 포기).
5. **1인 참가** — 팀원 롤 기술서 생략.
6. **아트 = 사용자의 Claude Design 목업 승계** — 목업 원본과 스프라이트 엔진을 `reference/mockup/`에 확보. 코드 정의 도트(AI 이미지 생성/CC0 병행은 불필요해짐).

## 2026-08-09 (아키텍트 결정)

7. **Phaser 철회 → DOM/CSS + Canvas 스프라이트** — 목업이 이미 이 방식으로 완성되어 있어 이식 비용이 최소. 러너 미니게임만 단일 canvas 루프. (초기 추천을 목업 발견 후 뒤집음)
8. **게임 수치는 100% 결정론적 룰 엔진** — LLM 출력은 대사·이름·피드백 문구만. 숫자에 관여 불가. seed 고정으로 재현 가능.
9. **LLM 어댑터 3단**: `pool`(커밋된 사전 생성 풀, 기본) → `proxy`(VITE_LLM_PROXY_URL 설정 시 라이브) → `direct`(dev 전용, .env 키). 타임아웃 2.5s 초과 시 즉시 pool 폴백.
10. **specs/ 분리 대신 통합 문서** — D-1, 문서 수 최소화. GAME_DESIGN/TECH_SPEC/ART_BIBLE 3개로 통합.
11. **저장소 public 즉시 생성 + 커밋 기록 시작** — 공고 요건 "커밋 기록 유지". gh 계정 minsub0922, 저장소명 `pixel-arcade-tycoon`.
12. **CI는 build+deploy만** — Playwright는 로컬 검증 전용 (D-1에서 CI 불안정성 배제).

## 미결 (막히면 이 문서에 추가하고 진행)

- Gemini 모델명은 `cloudflare/wrangler.toml` vars로 관리 (무료 티어 변동 대응).
- 오락실 기기 배치 UX: 슬라이스는 "카드 클릭 → 빈 슬롯 클릭" 우선, 드래그는 폴리시 단계.
