# QUALITY_BAR — 품질 기준

합격 = **자동 게이트 전원 통과 + 사람 승인 항목 전원 승인**. 점수제 항목은 10점 만점, 항목별 최소점 미달 시 전체 불합격.

## A. 자동 게이트 (npm run verify + 시나리오)

| # | 게이트 | 판정 |
|---|---|---|
| A1 | `npm run build` 성공, 타입 에러 0 | CI/로컬 |
| A2 | Vitest core 룰 테스트 전원 통과 | `npm run test` |
| A3 | 시나리오 S1~S7 통과 (PLAYTEST_SCENARIOS) | `npm run test:e2e` |
| A4 | **LLM 완전 차단(pool 모드)로 S1 완주** | S4 |
| A5 | 콘솔 에러 0 (경고 허용), soft-lock 0 (모든 상태에서 활성 버튼 ≥1) | S1·S6 중 수집 |
| A6 | 1.5사이클 도달 시 `telemetry.elapsedVirtualMs ≤ 180000` (step 가속 포함 가상 시간 기준) | S1 텔레메트리 |
| A7 | 첫 진입 → 첫 게임 완성: `telemetry.clicks ≤ 8` (코치마크는 클릭 소비 없음) & elapsedVirtualMs ≤ 60000 | S1 텔레메트리 |
| A8 | 데스크톱 1440×900·1280×720, 모바일 뷰포트(landscape)에서 렌더 깨짐 없음 | e2e 스크린샷 |
| A9 | dist 자산 합계 < 500KB(gzip 전, 목표 300KB) — `scripts/check-bundle.mjs` 가 verify 에 포함 | T13 배선 |
| A10 | `telemetry.spriteFallbackCount == 0` + DOM 텍스트에 "TODO" 0건 | S1 중 검사 |

## B. 점수제 (사람 + 에이전트 자기평가, 제출 전 1회)

| 항목 | 최소 | 만점 기준 |
|---|---|---|
| 핵심 선택의 의미 | 7 | 결정 3택이 실제로 다른 결과를 낳고, 그 차이가 화면에서 읽힌다 (S2 비교 스크린샷) |
| 피드백→개선 명확성 | 8 | 태그 집계→안건→개선→재방문 대사의 인과가 설명 없이 보인다 |
| 템포·조작감 | 7 | 대기 시간 3초+ 구간 없음, 모든 클릭에 즉각 반응(사운드/모션) |
| 시각 일관성 | 8 | 목업과 나란히 놓고 위화감 없음 (ART_BIBLE 금지 패턴 0) |
| AI 응답 품질 | 6 | 라이브 모드 대사가 컨텍스트(기획·성향·이력)를 반영, 어색한 문장 < 20% |
| 안정성 | 8 | S6(연속 5사이클) 크래시·수치 폭주 없음 |

## C. 사람(사용자)만 승인 가능

1. 최종 도트 룩 — 목업 대비 비교 스크린샷 세트 (`evidence/final-*.png`)
2. 대사 톤 (한국어 자연스러움, 캐릭터 개성)
3. 30~60초 영상 컷 구성
4. 제출 문서 2종 최종본
5. **baseline 이미지 변경**: e2e 스크린샷 기준 이미지를 갱신하는 커밋은 diff 사유를 커밋 메시지에 명시 — 사용자 확인 전 기준 완화 금지

## 판정 우선순위

기능 추가 < 버그 수정 < A 게이트 < 템포. 시간이 부족하면 **기능을 줄여서** 게이트를 지킨다.

스코프 축소 순서 (앞에서부터 자른다):
1. T12 모바일 매트릭스 축소 — landscape 스모크 1종만 유지
2. T8 러너 축소 — 웃긴 버그 연출·패럴랙스·S7 삭제, 고정 코스 20초 (TRYOUT 자체는 공고 요건 "직접 플레이"이므로 삭제 불가)
3. T11 SFX 8종 → 4종 (클릭·코인·환호·점프)
4. S6 연속 5사이클 → 3사이클
5. 최후에만: 특징 선택지 축소, 장르 4→2 (손님 4유형 반응 매트릭스가 데모의 본체이므로 데이터 축소는 마지막)
